# AI Gateway Configuration
# These two variables are injected by EdgeOne Makers at runtime; users only
# need to know the gateway credentials. Do NOT add BLOB_PROJECT_ID / BLOB_TOKEN
# here — storage is platform-managed and the .env should never ask for those.
AI_GATEWAY_API_KEY=your-api-key
AI_GATEWAY_BASE_URL=your-gateway-url
